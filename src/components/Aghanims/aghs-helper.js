export function getScepterIconPath (isEnabled) {
    return `/images/dota2/aghs_scepter_${isEnabled ? "on" : "off"}.png`;
}

export function getShardIconPath (isEnabled) {
    return `/images/dota2/aghs_shard_${isEnabled ? "on" : "off"}.png`
}